import { SceneCanvasState } from '../../types/internal/scene-canvas-internal.types';

export const InitialSceneCanvasState: SceneCanvasState = {
    isLocked: false,
    isSelectMode: false,
    selectedNodeId: undefined
};
